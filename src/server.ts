function welcome(name: string) {
  console.log('welcome', name);
}
const user = {
  name: 'bhoomi',
};

const classname = {
  sub: 'maths',
};

welcome('bh');

const U1 = user.name;
const v1 = classname.sub;
console.log(U1 + v1);
